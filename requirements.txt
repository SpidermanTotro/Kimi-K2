# THE FORGE + KIMI K2 Requirements

# ── Core REST API server ────────────────────────────────────────────────────
Flask>=2.3.0
Flask-CORS>=4.0.0

# ── NullClaw — local AI programming agent ──────────────────────────────────
# Uses only stdlib by default (subprocess, urllib, re, json, pathlib).
# Install Ollama separately: https://ollama.com/download
# Then pull a model:  ollama pull qwen2.5-coder:7b

# ── GeminiProgramFixer — free cloud fallback ───────────────────────────────
# Free API key: https://aistudio.google.com/apikey
google-generativeai>=0.7.0   # optional: only needed for Gemini fallback

# ── AI Reconstructor / ELF X-Ray ───────────────────────────────────────────
# Uses only stdlib (struct, mmap, re, pathlib) — no extra deps needed.

# ── HTTP requests (used by OllamaClient REST calls + binary tools) ─────────
requests>=2.31.0

# ── Optional: vLLM for bigger local models on GPU ──────────────────────────
# pip install vllm           # run 13B+ models on RTX 5060 Ti
# pip install sglang         # alternative inference server

# ── Optional: enhanced semantic search ────────────────────────────────────
# pip install sentence-transformers faiss-cpu   # embed_repo / search_semantic

# ── Optional: FastAPI web interface ────────────────────────────────────────
# pip install fastapi uvicorn pydantic          # for nullclaw web server
